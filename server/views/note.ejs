<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note Details</title>
    <style>
        /* CSS styles for note page */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }

        .note-details {
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 5px;
            background-color: #fff;
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1><%= note.title %></h1>
    <p><%= note.description %></p>
    <p>Tags: <%= note.tag %></p>
    <p>Date: <%= note.date %></p>
    <p>Color: <%= note.color %></p>
    <p>Pinned: <%= note.pinned %></p>
    <p>Archive: <%= note.archive %></p>
    <p>Trash: <%= note.trash %></p>
    <a href="/api/v1/notes/<%= note._id %>edit">Edit</a>
    <form action="/api/v1/notes/<%= note._id %>?_method=DELETE" method="post">
        <button type="submit">Delete</button>
    </form>
</body>
</html>
